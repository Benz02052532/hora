/* ============================================================
   HORA — ตั้งค่าระบบ
   แก้ไขไฟล์นี้ไฟล์เดียว ไม่ต้องแตะไฟล์อื่น
   ============================================================ */

window.HORA_CONFIG = {

  /* ---------- ชื่อบริษัท (แสดงบนหัวเว็บ) ---------- */
  companyName: 'บริษัทของคุณ',
  companyTagline: 'ผังองค์กร · โหราศาสตร์ไทย',

  /* ---------- รหัสผ่านเข้าเว็บ ---------- */
  /* ⚠️ รหัสนี้อยู่ในโค้ดฝั่งผู้ใช้ ใครเปิด view-source จะเห็นได้
     คุณรับทราบและเลือกวิธีนี้แล้ว (ดูหัวข้อ "ความปลอดภัย" ใน docs/SETUP.md) */
  password: 'hora2569',

  /* ---------- Supabase ---------- */
  /* ถ้ายังไม่ได้ตั้งค่า ให้ปล่อยว่างไว้ ระบบจะเก็บข้อมูลในเครื่อง (localStorage) ให้ก่อน
     วิธีเอาค่ามาใส่ อ่าน docs/SETUP.md ข้อ 2 */
  supabaseUrl: 'https://twwuwtzrqjwejbnommrd.supabase.co',
  supabaseAnonKey: 'sb_publishable_gYOkxzsyEDrDwUtttpf-rA_u-yCkjM_',

  /* ---------- พิกัดสำหรับคำนวณท้องฟ้า ---------- */
  /* ค่าเริ่มต้น = กรุงเทพฯ  เปลี่ยนเป็นจังหวัดคุณได้ */
  latitude: 13.7563,
  longitude: 100.5018,

  /* ---------- ลูกเล่นภาพ ---------- */
  visual: {
    starCount: 2400,        // จำนวนดาวพื้นหลังเสริม (ดาวจริงมีอยู่แล้วต่างหาก)
    showConstellations: true,// เส้นกลุ่มดาว
    showMilkyWay: true,      // แถบทางช้างเผือก
    shootingStars: true,     // ดาวตก
    parallax: true,          // ภาพขยับตามเมาส์
    skySpeed: 120,           // ความเร็วหมุนท้องฟ้า (1 = จริง, 120 = เร็วขึ้น 120 เท่าให้เห็นการเคลื่อนไหว)
    zodiacWheel: true        // วงล้อจักรราศีหมุนด้านหลัง
  },

  /* ---------- แสดงบรรทัดคำนวณโหราศาสตร์ในการ์ดพนักงาน ---------- */
  /* คุณเลือก "ข้อมูลดิบก่อน" — ถ้าวันหลังอยากเปิด เปลี่ยนเป็น true ได้เลย */
  showComputedAstrology: false
};
