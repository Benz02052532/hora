-- ============================================================
-- HORA — เพิ่มช่องบันทึกคำทำนาย
-- วิธีใช้: copy → วางใน Supabase > SQL Editor → Run
-- ปลอดภัยกับข้อมูลเดิม (ใช้ IF NOT EXISTS)
-- ============================================================

alter table public.employees
  add column if not exists prediction text;

-- ============================================================
-- เสร็จแล้ว! ควรเห็น "Success. No rows returned"
-- ============================================================
